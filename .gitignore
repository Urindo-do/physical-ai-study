# 저장소에 올리지 않는 파일들

# 1회용 로컬 안내 문서 (git 사용법 익히는 용도로 만든 것, 저장소에 남길 필요 없음)
# 절차는 CLAUDE.md로 대체되어 이 파일은 삭제함. 현재 작업 환경 규칙은 CLAUDE.md §2.
# ※ 초기 버전에 "리눅스 노트북에서 push"라고 적혀 있었고, 당시 규칙(문서·git은 전부 윈도우)
#    으로는 틀린 설명이라 여기에 그렇게 메모해뒀었다. 2026-09-22에 규칙이 바뀌어 지금은
#    리눅스 노트북에서 push하는 게 맞다 — 결과적으로 그 문장이 맞는 말이 됐다.
PUSH_GUIDE.md

# HTML 체크리스트 — 브라우저 저장 기능이 없어 새로고침하면 체크가 초기화됨.
# 진행 기록은 docs/ros2-r2r-checklist.md 하나로 단일화했으므로 저장소에서는 제외.
docs/ros2-r2r-checklist.html
