# physical-ai-study — 에이전트용 규칙 문서

이 파일은 이 저장소에서 작업하는 AI 에이전트(Claude Code 등)가 항상 읽는 규칙이다.
사람이 매번 상황을 설명하지 않아도 되도록, 여기에 저장소 구조와 작업 규칙을 고정해둔다.

---

## 1. 이 저장소는 무엇인가

Physical AI(로봇 + AI) 학습 기록 저장소. 코드 프로젝트가 아니라 **학습 로그 아카이브**다.
Ubuntu/ROS 2부터 시작해 3D 비전 센서 · MoveIt · Gazebo로 이어지는 커리큘럼을 따라가며
공부한 내용, 실행한 명령어, 겪은 문제와 해결 과정을 날짜별로 쌓는다.

## 2. 작업 환경 (중요 — 헷갈리기 쉬움)

| 기기 | 역할 |
|---|---|
| **윈도우 PC** | **문서 작업 전부.** 이 저장소의 clone이 여기 있고, git 작업도 여기서 한다 |
| **리눅스 노트북** (Ubuntu 22.04) | **ROS 2 실습 전용.** 문서 작업·git 작업은 하지 않는다 |

→ 즉 학습은 리눅스에서 하고, **그 기록을 남기는 일은 전부 윈도우에서** 한다.
→ 이 저장소를 리눅스에 clone하지 않는다.

**저장소 위치**: `C:\Users\rlaeo\physical-ai-study` (2026-08-22 clone 완료, Cowork 세션에 폴더 연결됨)

## 3. 디렉터리 구조와 배치 규칙

이 저장소는 **성격이 다른 네 개의 축**으로 나뉜다. 새 문서를 놓을 때는 먼저 어느 축인지 정한다.
앞의 세 축은 **내가 정한 것**을 담고, `assignments/`만 **밖에서 요구받은 것**을 담는다.

| 폴더 | 축 | 담는 것 | 수정 정책 |
|---|---|---|---|
| `study-log/` | **시간축** | 그날 무엇을 했나 (날짜별 기록) | 사후 수정 금지 (§7) |
| `docs/` | **진행축** | 진도 관리용 체크리스트·계획 | 계속 갱신 |
| `notes/` | **지식축** | 주제별 개념 정리 (날짜 무관, 계속 찾아보는 레퍼런스) | 계속 보강 |
| `assignments/` | **과제축** | 연구실에서 내준 과제와 그 수행 결과물 | 과제 종료 시 동결 |

```
physical-ai-study/
├── CLAUDE.md                  ← 지금 이 파일 (에이전트 규칙)
├── README.md                  ← 전체 개요 + 진행 상황 요약
├── .gitattributes             ← 줄바꿈 LF 고정 (§3-1)
├── docs/                      ← [진행축] 진도 관리 체크리스트
│   └── ros2-r2r-checklist.md      R2R 강좌 137개 체크리스트 (단일 진실 공급원)
├── notes/                     ← [지식축] 주제별 개념 정리
│   └── 주제명.md                   예: apqp-robot-mass-production.md
├── assignments/               ← [과제축] 연구실(SIBL) 과제와 결과물
│   ├── README.md                  과제 목록 인덱스
│   └── 과제-주제/                  하나의 과제 = 하나의 폴더 (폴더명에 날짜 없음)
│       ├── practice-log.md            수행 기록 본문
│       └── images/                    기록에 삽입되는 스크린샷
└── study-log/                 ← [시간축] 날짜별 학습 기록
    ├── images/                    로그에 삽입되는 스크린샷
    └── YYYY-MM-DD-*.md            학습 로그
```

**새 파일을 놓을 위치**
- 날짜별 학습 로그 → `study-log/YYYY-MM-DD-dayN-주제.md`
- 로그에 들어갈 이미지 → `study-log/images/YYYY-MM-DD-설명.png`
  (로그에서는 상대경로 `./images/파일명.png`으로 참조)
- 진행 중 계속 고쳐 쓰는 참고 문서 → `docs/`
- **주제별 지식 정리 → `notes/영문-주제명.md`**
  날짜를 파일명에 넣지 않는다. "그날 뭘 했나"가 아니라 "그 주제가 무엇인가"를 담기 때문이다.
- **연구실 과제 수행 결과 → `assignments/과제-주제/practice-log.md` (+ `images/`)**
  폴더명에 날짜를 넣지 않는다. 과제는 며칠에 걸쳐 진행되므로 특정 날짜에 묶이지 않는다.
  과제 하나를 마치면 `assignments/README.md`의 과제 목록 표에 한 줄 추가한다.

**`README.md`라는 이름은 안내판에만 쓴다.**
저장소 대문(`/README.md`)과 폴더 목록 인덱스(`assignments/README.md`)처럼 **"여기가 뭐 하는 곳인지"**
알려주는 문서에만 붙인다. 내용이 담긴 본문 문서는 제 이름을 갖는다(`practice-log.md` 등).
> 경위: 1,400줄짜리 실습 기록을 `README.md`로 뒀더니 대문·인덱스·본문이 전부 같은 이름이 되어
> 어느 게 안내판인지 구분이 안 됐다. (2026-09-03에 되돌림)

**어느 축인가 — 판단 기준**
- **완료 기준을 내가 정했나, 밖에서 정해줬나?** 밖에서 정해준 것(과제 문서·평가자가 있음) → `assignments/`
- 나중에 **"그때 뭐 했더라"** 로 찾을 것 같으면 → `study-log/`
- 나중에 **"그거 뭐였더라"** 로 찾을 것 같으면 → `notes/`
- 한 수업에서 둘 다 나오면 나눠 쓴다. 로그에는 그날의 진행과 시행착오를, 노트에는 정리된 개념을 담는다.
- `notes/` 문서는 나중에 계속 보강하므로, 문서 안에 "다음에 할 일" 항목을 남겨둔다.

### 3-1. 줄바꿈 규칙 (.gitattributes)

`* text=auto eol=lf` 가 걸려 있다. 저장소 안에는 항상 **LF**로 저장된다.
윈도우 편집기가 파일을 CRLF로 바꿔 저장해도 git이 커밋 시 LF로 되돌리므로 가짜 diff가 생기지 않는다.

> 도입 경위: 2026-08-30에 추적 파일 9개가 **내용은 한 글자도 안 바뀌었는데** 전부 "수정됨"으로
> 떠 있었다. 원인은 CRLF 변환뿐이었고, 진짜 변경분(README·체크리스트)이 그 노이즈에 묻혀 있었다.

## 4. 진행 기록 방식

- **진도는 `docs/ros2-r2r-checklist.md`의 체크박스로만 관리한다.**
  `- [ ]` → `- [x]`로 바꾸고 커밋하면 git log 자체가 진행 기록이 된다.
- 체크리스트 상단의 진행률 표에 그날 완료 개수와 누적을 한 줄 추가한다.
- **날짜별 일정은 넣지 않는다.** (한 번 시도했다가 계획 대비 실적이 크게 어긋나 제거함.
  이유는 체크리스트 파일 상단에 기록되어 있다.)
- 같은 사실이 README와 체크리스트 양쪽에 있으면 **둘 다 갱신**한다. 특히 README의
  "진행 상황 요약"과 "저장소 구조" 블록.

## 5. 문서 작성 원칙

- **확인한 것과 추정한 것을 구분해서 쓴다.** 확실하지 않으면 "미확인"이라고 명시하고,
  그럴듯하게 지어내지 않는다.
- 강의 필기를 정리할 때 **사실관계가 틀린 부분은 정정하고, 정정했다는 사실을 남긴다.**
  (예: "Noetic은 ROS 2가 아니라 ROS 1의 마지막 배포판" 같은 메모)
- 명령어가 나오면 **뜻 → 옵션 뜻 → 출력 해석 → 윈도우와 비교** 순으로 풀어서 쓴다.
  작성자는 리눅스 입문자이므로 생략하지 않는다.
- 로그 끝에는 **회고(계획 대비 실적, 원인)** 와 **다음에 할 일**을 남긴다.

## 6. 커밋 규칙

- 커밋 메시지는 한국어. 형식: `<범위>: <무엇을 했는지 한 줄>`
  - 예: `Day5: 입문 2-3~3-9 진행 (노드/토픽/서비스)`
  - 예: `체크리스트: Day5 진도 반영`
  - 예: `README: 진행 상황 갱신`
- **성격이 다른 변경은 커밋을 나눈다.** (학습 로그 추가 / 체크리스트 갱신 / README 갱신)
- 브랜치는 쓰지 않는다. `main`에 직접 커밋한다. (혼자 쓰는 학습 저장소)

## 7. 하지 말 것

- `docs/ros2-r2r-checklist.html` 커밋 금지 — 브라우저 저장 기능이 없어 체크가 초기화된다.
  진행 기록은 `.md` 하나로 단일화하기로 결정했다. (`.gitignore`에 등록되어 있음)
- `PUSH_GUIDE.md` 커밋 금지 — 1회용 로컬 안내 문서. (`.gitignore`에 등록되어 있음)
- 학습 로그의 **과거 기록을 사후에 고치지 않는다.** 그때 그렇게 알았다는 것 자체가 기록이다.
  정정할 내용이 생기면 새 로그에 "이전 기록 정정" 항목으로 남긴다.

## 8. 자주 하는 작업

**받은 파일들을 저장소에 반영하고 올리기**
1. 파일을 §3 규칙대로 배치
2. 체크리스트 진행률·체크박스 갱신 (§4)
3. README 진행 상황 갱신이 필요한지 확인 (§4)
4. §6 규칙대로 커밋 나눠서 `git add` → `git commit` → `git push`
